# 开发约定

- 项目根目录是本文件所在目录，入口为 `start.bat` / `app.py`，命令不添加旧 `Project/` 前缀。
- 产品、存储和交付约定见 `Documents/` 中的三份说明；当前阶段为 S7 打包与发布验收。
- 使用独立 UI 与模型解释器；保持已验证的 v2Pro 引擎内部布局。GPU 作业串行，只管理本项目拥有的进程。
- 不修改或删除用户音频、音色、成品及本地配置，除非用户明确要求。业务删除必须经过既有归属校验和事务流程。
- 文档仅保留使用、产品设计、架构和开发交付说明。不要重新建立成员分工、个人周志或历史证据文档树。
- 诊断结果、截图和原始命令输出写入 `data/logs/diagnostics/` 或 `data/tmp/`，默认不进入交付包。公开说明采用项目相对路径和通用示例。
- 保留第三方许可证、来源和资源校验清单，不把第三方代码或模型标为自研。
- 按修改范围运行有效验证；静态检查、测试替身、真实 GPU 运行和人工听测分别说明，不将未验证项标为通过。
- 发布使用独立 staging 和明确包含清单。默认排除本地数据、私人配置、归档、Git 元数据、缓存及诊断报告。
