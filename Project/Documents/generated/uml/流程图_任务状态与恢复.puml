@startuml
skinparam defaultFontName Microsoft YaHei
|系统|
start
:读取任务索引和阶段日志;
if (状态转换是否合法?) then (是)
  :更新时间、阶段和消息;
  if (输出文件存在?) then (是)
    :保留任务终态;
  else (否)
    :标记 OUTPUT_INVALID;
  endif
else (否)
  :返回 INVALID_TASK_STATE;
endif
:原子写入 JSON 索引;
stop
@enduml
