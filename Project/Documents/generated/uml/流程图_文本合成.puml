@startuml
skinparam defaultFontName Microsoft YaHei
|项目使用者|
start
:选择音色、文本和情绪;
|系统|
:校验文本范围和参数;
:校验权重与参考 WAV;
if (资源完整?) then (是)
  :获取 GPU 锁;
  :启动 GPT-SoVITS API;
  :轮询本地合成接口;
  :写入 WAV;
  if (WAV 有效?) then (是)
    :写入 GenerationRecord;
  else (否)
    :返回 OUTPUT_INVALID;
  endif
  :终止子进程并释放锁;
else (否)
  :返回 VOICE_WEIGHTS_MISSING 或 REFERENCE_MISSING;
endif
stop
@enduml
