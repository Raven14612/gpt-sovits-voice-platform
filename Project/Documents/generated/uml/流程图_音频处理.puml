@startuml
skinparam defaultFontName Microsoft YaHei
|项目使用者|
start
:选择授权音频;
|系统|
:解析绝对路径;
if (路径和格式有效?) then (是)
  :创建数据集任务;
  :执行固定参数切片;
  :记录日志和退出码;
  if (切片输出存在?) then (是)
    :写入 DatasetRecord;
  else (否)
    :标记 OUTPUT_INVALID;
  endif
else (否)
  :返回 INVALID_AUDIO;
endif
stop
@enduml
