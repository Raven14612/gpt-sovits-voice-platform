@startuml
skinparam defaultFontName Microsoft YaHei
|开发者|
start
:提交特征任务;
|系统|
:校验校对数据集;
if (GPU 空闲?) then (是)
  :获取全局锁;
  :执行特征提取命令;
  :检查特征输出;
  if (输出完整?) then (是)
    :任务 SUCCEEDED;
  else (否)
    :任务 FAILED / OUTPUT_INVALID;
  endif
  :释放全局锁;
else (否)
  :返回 GPU_BUSY;
endif
stop
@enduml
