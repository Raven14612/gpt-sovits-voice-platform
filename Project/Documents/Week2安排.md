# Week 2 任务安排：50 系数据与训练任务闭环

生效日期：2026-09-07。40 系任务继续暂停。

## CXY：50 系算法与验收

- 冻结切分、ASR、特征提取、SoVITS/GPT 训练参数和版本。
- 用当前真实数据再跑一次可复现的完整训练闭环，记录配置、耗时、权重和日志。
- 审核 `Columbina` 音色档案字段、参考音频和权重配对。
- 验证训练权重回归合成，确认 adapter 输出与整合包结果均可播放。

## LHY：数据档案、训练编排与索引

- 实现 DatasetRecord 的 JSON 索引读写和原子替换。
- 实现数据处理任务编排：validating、slicing、asr、feature_text、feature_hubert、feature_semantic。
- 接入已冻结的 adapter 命令边界，保留参数数组、日志、退出码和超时处理。
- 实现训练任务编排、权重归档、恢复和失败状态；不得生成假权重或假成功。
- 为新增功能补充无 GPU 测试和故障测试替身。

## WGX：音频页与训练页

- 音频页接入 service 接口：上传、切片、ASR、文本校对和人工情绪标签空状态。
- 训练页显示真实任务阶段和日志摘要，不显示虚构百分比。
- 接入 DatasetRecord/TaskRecord 的选择和刷新；GPU_BUSY 时禁用重复提交并说明原因。
- 完成窄窗口、键盘和刷新恢复的浏览器冒烟记录。

## LJQ

- 本周不执行 40 系任务；不得修改 50 系主线。

## Week 2 过线标准

- 从当前数据集通过项目任务编排生成可加载的 GPT/SoVITS 权重。
- 项目正确保存数据集档案、音色档案、权重路径和任务日志。
- 失败、取消、GPU_BUSY 和重启恢复状态可测试复现。
