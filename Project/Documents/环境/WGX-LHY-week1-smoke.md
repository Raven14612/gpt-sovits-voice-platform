# WGX/LHY Week 1 冒烟记录

## 结构检查

| 检查项 | 实际结果 | 结论 |
|---|---|---|
| 唯一入口 `app.py` | Gradio 5.50.0 已安装，应用可启动 | 通过 |
| 默认页面 | 音频数据处理页 visible=True | 通过（代码检查） |
| 四页切换 | 四个按钮分别更新四个页面 visible 状态 | 通过（代码检查） |
| 环境状态 | 侧栏底部显示 `probe_engine()` 实时状态 | 通过 |
| 空状态 | 无数据集/音色/结果时明确提示，按钮不产生假成功 | 通过 |
| 任务状态组件 | 支持等待、执行、成功、失败、取消和日志路径 | 通过 |
| GPU_BUSY | 全局锁第二次占用返回明确错误 | 通过（单元测试） |
| 窄窗口、键盘、刷新 | 未做逐项人工浏览器交互记录 | 待补 |

## 真实 adapter 验证

项目适配器使用 50 系训练权重完成真实合成：

- 输出：`Project/data/outputs/cxy_adapter_20260907.wav`
- 日志：`Project/data/outputs/cxy_adapter_20260907.log`
- 结果：WAV 文件生成，退出时 API 子进程已清理
