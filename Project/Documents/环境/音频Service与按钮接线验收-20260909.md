# 音频 Service 与按钮接线验收

日期：2026-09-09。范围：完整 PCM WAV 导入、可选范围裁剪、真实切分、全部切片 ASR、标注校验及页面回填。特征提取和新模型训练不在本次执行范围内。

## 真实运行结果

- 输入：已有 `D:/Documents/Columbina-Hyposelenia.wav`，58.93224489795919 秒、双声道 PCM WAV。它不是 Citlali 音频。
- 通过项目页面依次上传、保存音频、点击“切分并识别”，起点 0，保留到结尾。
- 使用已配置的 50 系整合包解释器，工作目录为整合包根目录，复用已有 FunASR 模型。
- 验收任务：`audio-5b4e8c7ad3dc48e0b9d0b0fe57228ff0`。
- 真实切分退出码 0，生成 12 个 WAV；真实 ASR 退出码 0，生成 12 条非空标注。
- Service 检查切片路径集合与标注集合完全一致，成功后数据集变为 `transcribed`。识别正确率仍需人工校对，不能因流程成功而标为 `reviewed`。
- 浏览器滚动检查了全部 12 条记录，并验证切片选择可以回填校对框。处理期间按钮禁用，结束后恢复；下拉框及数据集状态同步刷新。
- 1440×1000 桌面及 390×844 手机截图完成，未发现页面横向溢出或 JavaScript 错误。

验收采用隔离的临时数据集和索引，关闭测试后清理；不替换项目已有 `citlali-w2-wr-test` 或正式 Citlali 音色。测试记录中的临时路径是当时的执行路径，不是现在可用的正式训练输入。

## 实现与保护

- `adapters/gpt_sovits.py`：真实切分和 FunASR 参数数组、解释器环境及入口检查。
- `services/audio_service.py`：完整流水线、范围校验、独立任务目录、任务状态持久化、全流程 GPU 互斥及失败释放。
- `services/pipeline_service.py`：支持传入整合包进程环境；保存命令、工作目录、退出码、标准输出和错误。日志在各阶段结束后写入，目前不是逐行实时日志。
- `ui/audio_page.py`：生成任务 ID 后先更新页面，再执行 Service；成功后加载全部识别文本，失败保留错误信息并解除提交中状态。
- 每次重新处理均写入新的 `processing/<task-id>/`。失败保留上次有效数据集结果，成功重跑则需要重新校对。

## 检查与证据

- `python -m unittest discover -v`：56/56 通过，无跳过。设置真实音频和 Citlali 权重环境变量供测试读取；这些单元测试中的子进程替身属于工程测试，不代表新训练成功。
- `python -m compileall -q adapters services ui tests scripts app.py`：通过。
- `git diff --check`：通过。
- 浏览器真实执行：`scripts/wgx_smoke_server.py`，环境变量 `AUDIO_SERVICE_SMOKE=1`、`WGX_REAL_AUDIO=D:/Documents/Columbina-Hyposelenia.wav`；需要 Node 可解析 `playwright/test`，以及本机 Edge。
- 浏览器结果与截图：`Documents/环境/evidence/audio-service/result.json`、`01-running.png`、`02-success-desktop.png`、`03-success-mobile.png`。
- 本机完整运行日志：`data/logs/audio-acceptance/audio-5b4e8c7ad3dc48e0b9d0b0fe57228ff0-slicing.log` 和 `audio-5b4e8c7ad3dc48e0b9d0b0fe57228ff0-asr.log`。
- 本机记录及原始识别文本：`data/logs/audio-acceptance/records.json`、`asr.list`。`data/logs/` 按仓库规则不提交。
- 异常回归涵盖空切片、ASR 漏标、空文本、重复或外部路径、退出码非零、超时、无效裁剪、GPU 占用以及重复任务 ID。

本次没有执行特征提取、SoVITS/GPT 新训练或音色归档。后续流程仍需接入相应真实命令，并在用户决定恢复新训练后再运行。
