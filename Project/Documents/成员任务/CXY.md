# CXY 任务单：产品、50 系引擎与最终验收

> 角色定位：项目负责人和最终决策人；领导产品范围、50 系真实算法主轨与阶段验收。  
> 不再包揽 LHY 的后端实现或 WGX 的前端实现。

## 当前任务队列

## 现在立刻做什么

先不要写新页面，也不要安装新整合包。你可以继续只用 Web UI，但必须按 [Web UI 整合包取证指南](../环境/WebUI整合包取证指南.md) 留证：用现有 `xxx-e15.ckpt` 和 `xxx_e8_s200.pth` 生成一条**新的** WAV，截图保存页面输入和权重路径，复制控制台日志，再从输出目录确认新文件。不要猜命令；把页面动作对应的源码位置和真实结果发给 LHY，让他据此接通 adapter。

### CXY-W1-01：50 系训练权重推理补证（P0）

- **输入**：现有 `xxx-e15.ckpt`、`xxx_e8_s200.pth`、合法参考切片及对应文本。
- **工作**：使用关闭并行推理的配置生成一条全新 WAV。先记录 Web UI 页面字段和截图，再从启动脚本、`webui.py`、控制台日志和前后文件列表反查命令、参数、耗时、显存和输出时间；无法确认的内容标记“待实测”。
- **产物**：本机证据目录中的 WAV 与日志；提交仓库的脱敏记录文档。
- **通过**：WAV 可播放；页面截图、权重文件名/修改时间、控制台日志和输出文件能相互对应，证明加载的是选定训练权重。只有页面成功提示而没有文件证据，算未验证。
- **下游**：LHY-W1-02、CXY-W1-02。

**可直接交给 AI 的提示词**

```text
你是本地 Windows GPT-SoVITS 调试助手。请先阅读：
1. Project/Documents/当前执行计划 v3.md
2. Project/Documents/契约/engine-adapter-v0.1.md
3. Project/Documents/成员任务/CXY.md

我的 50 系运行时根目录、Python 路径、GPT 权重、SoVITS 权重、参考音频和参考文本会在下面提供：
<逐项填写真实绝对路径>

目标：先用 Web UI 完成并取证一条全新的 WAV，再根据页面字段和源码位置反查训练后的 GPT + SoVITS 权重调用。关闭并行推理，GPU 一次只跑一个任务。

请输出：
- 如果源码和日志足够，给出当前版本的最小可执行命令或 Python 调用；如果不足，先给出 Web UI 取证步骤，并明确标记“待实测”，不要猜命令；
- 每个参数的用途；
- 输出文件应该出现在哪里；
- 如何通过页面字段、文件名/修改时间和日志确认加载的是训练权重而不是预训练权重；
- 如何记录耗时、显存、退出码和日志；
- 失败时优先检查的 5 项。

禁止：猜测路径或 API、编造成功、使用旧 WAV 充当本次结果、修改 GPT-SoVITS 核心代码、开启并行推理。Web UI 成功提示不能替代 WAV 和日志证据。
只给出方案和检查步骤，不要替我宣称已经成功；成功必须以我本机真实 WAV 和日志为准。
```

### CXY-W1-02：冻结引擎命令与 I/O（P0）

- **输入**：上一任务证据和整合包真实入口。
- **工作**：整理探测、切分、ASR、特征、SoVITS 训练、GPT 训练、推理的最小命令；标出工作目录、必填输入、输出文件和失败条件。
- **产物**：对 `契约/engine-adapter-v0.1.md` 的审阅结论和真实命令附录。
- **通过**：LHY 无需读取 WebUI 即可实现适配器；不存在个人路径硬编码要求。
- **下游**：LHY-W1-02/W2-01、LJQ-W1-02。

**可直接交给 AI 的提示词**

```text
请阅读 Project/Documents/契约/engine-adapter-v0.1.md 和当前 GPT-SoVITS 源码，帮助我整理 adapter 真实 I/O。

我会提供真实版本、入口文件和已经成功运行的命令/日志：
<粘贴版本、命令、日志摘要和路径>

请按 probe_environment、slice_audio、run_asr、extract_features、train_voice、synthesize 六项输出：
1. 必填输入；
2. 可选输入；
3. 输出文件和目录；
4. 成功判定；
5. 失败退出码/错误文本；
6. 不能写死的个人路径；
7. 给 LHY 的最小接口说明。

如果源码或日志不足以确认，请标记“待实测”，不要猜 API。不要复制上游大段源码，不要设计 FastAPI，不要开启并行推理。
```

### CXY-W1-03：Week 1 联调验收

- **输入**：LHY 后端骨架、WGX 四页壳、LJQ 40 系基线。
- **工作**：逐项检查唯一入口、四页切换、adapter 探测、50 系真实 WAV 和 40 系环境结论。
- **产物**：验收表、证据索引和 `Decisions.md` 阶段结论。
- **通过**：每项结论均有文件、日志、音频或截图；Mock 只标作测试。

**可直接交给 AI 的提示词**

```text
请根据 Project/Documents/当前执行计划 v3.md、契约文件和四人任务单，生成 Week 1 联调验收表。

现有证据如下：
<填写真实文件、日志、WAV、截图路径>

验收对象：唯一 app.py、四页切换、环境探测、50 系真实推理、40 系环境结论、后端导入测试。
请输出表格：验收项、操作步骤、预期结果、证据路径、通过/部分通过/阻塞、负责人、下一步。

规则：Mock 只能标为测试证据；没有文件或日志就标“未验证”；不要把计划文字当完成证据；40 系未完成训练不能否定 50 系主轨。
```

## Week 2–4 领导任务

- Week 2：冻结算法参数；执行 50 系完整训练；审核音色档案。
- Week 3：完成训练权重回归、中性/人工情绪参考对照和连续合成。
- Week 4：主持最终验收、Demo、范围声明和答辩证据归档。

## 本周不要做

- 不替 LHY 实现所有 services。
- 不替 WGX 写完整四页。
- 不为 40 系结果作无证据保证。
- 不继续从零安装另一套 Python 环境；优先固化已验证运行时的调用边界。
