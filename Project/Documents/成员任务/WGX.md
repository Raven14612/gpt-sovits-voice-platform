# WGX Week 3 任务单：合成与结果交互

## WGX-W3-01：合成页任务交互（P0）

- 输入：`tts-history-v1`、合成 TaskRecord、可用音色列表。
- 工作：实现按钮等待/执行/失败/成功状态、防重复提交、真实参数控件和当前 WAV 展示。
- 通过：未选音色/空文本有明确提示；GPU_BUSY 禁用；参数能力与契约一致；不显示假百分比。

**AI 节点提示词**

```text
依据 tts-history-v1 修改 tts_page，只通过 services 提交任务。接入音色、人工情绪参考、文本、真实支持的高级参数、按钮状态和 AppError；执行时防重复提交，成功展示返回 WAV，失败清空旧结果。补充回调测试和 Gradio 构建检查。
```

## WGX-W3-02：结果页下载、删除与复用（P0）

- 输入：history CRUD/reuse 接口、共享 State。
- 工作：成功后切到结果页；实现播放、下载、删除和“复用参数”回填合成页。
- 通过：删除有明确目标且刷新列表；复用完整回填文本/音色/情绪/参数；失败记录不展示。

**AI 节点提示词**

```text
修改 result_page 与 layout 共享状态：合成成功自动显示当前结果；结果可播放、下载、删除；复用后回到合成页并回填所有 GenerationRecord 参数。删除和读取只能调用 history_service。覆盖空历史、文件缺失、删除当前项和复用回填测试。
```

## WGX-W3-03：浏览器回归与截图（P0）

- 输入：集成后的四页应用。
- 工作：验证桌面/390px、键盘、长中文、连续提交、失败、删除、复用和刷新恢复。
- 产物：`Documents/环境/Week3-browser-smoke.md` 和截图。
- 通过：无重叠/白屏/旧结果误显示；主要流程可用键盘完成；控制台无未处理异常。

**AI 节点提示词**

```text
启动 start.bat，对 Week3 合成与结果闭环做桌面和 390px 浏览器回归。覆盖键盘、长文本、GPU_BUSY、服务失败、成功结果、下载、删除、复用和刷新。截图必须来自当前页面并附操作与实际结果；检查控制台异常，不得用静态截图代替交互。
```
