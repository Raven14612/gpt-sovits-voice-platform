from __future__ import annotations

import gradio as gr


def render_root_layout() -> None:
    gr.Markdown("# AI 音频生成")
    gr.Markdown(
        "Week 1 `LHY-N1` 工程骨架已建立。当前仅保留单入口、基础样式和后续集成所需目录。"
    )
